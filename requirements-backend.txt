# SP-2 backend (local FastAPI). Separate from requirements.txt (SP-1 training stack)
# so the laptop backend env stays lean — no torch/transformers needed locally.
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
pillow==10.4.0
jiwer==3.0.4
# SP-5 object storage: minio-py is the official MinIO Python client (S3-compatible).
# Pinned to 7.2.x which is the latest stable series as of mid-2026 and supports
# presigned URLs via timedelta (required by objectstore.py).
minio==7.2.7
# SP-5 metadata store (eval runs / uploads). psycopg v3 is imported lazily by htr_sp5.store
# (and htr_sp3.store), so the /v1/eval/* and /v1/uploads endpoints — which the frontend
# dashboard & history pages call — need it installed at runtime.
psycopg[binary]>=3.1
# Lets config.py read HTR_* from a local .env when present (containers inject env directly,
# but this keeps `uvicorn ... --app-dir src` behaving the same locally).
python-dotenv>=1.0
# QUICK FIX (RAG): when HTR_ENABLE_RAG=1, htr_sp2.corrector_factory builds the gate vocabulary
# via htr_sp1.data.load_iam_splits(), which imports `datasets` and downloads the IAM-train split
# from the Hub on first use. Pinned to match requirements.txt (SP-1 stack). NOTE: this is the
# fast path, not the lean one — the proper fix is to read the train words back from the pgvector
# store instead of re-downloading the dataset at serve time (no `datasets` dep on the API tier).
datasets==2.20.0
# Dev / test only:
pytest==8.2.2
pytest-httpx==0.30.0
