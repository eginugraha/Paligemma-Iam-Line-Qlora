# Base URL of the SP-2 HTR backend that serves POST /v1/detect.
# Defaults to http://localhost:8000 when unset (see src/lib/api.ts).
# Copy this file to `.env` and adjust if the backend runs elsewhere.
VITE_API_BASE=http://localhost:8000
