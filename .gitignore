node_modules/
__pycache__/
*.pyc
.env
.venv/
.DS_Store
checkpoints/
*.gguf
*.safetensors

# Superpowers visual brainstorm scratch dir
.superpowers/

# Local-only docker compose override (publishes ports + local CORS/API base).
# MUST NOT reach the server — compose auto-merges it and would break production.
docker-compose.override.yml
